#include<stdio.h>
void main(){
float km,miles;
    printf("Enter distance in km : \n");
    scanf("%f",&km);

    (miles = 0.621 * km);

    printf("Same distance in miles is equal to : %.2f\n",miles);
    
}